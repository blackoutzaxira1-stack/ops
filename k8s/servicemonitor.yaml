apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: devops-node-app
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: devops-node-app
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
