apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-node-app-config
  namespace: default
data:
  PORT: "3000"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "devops_db"
