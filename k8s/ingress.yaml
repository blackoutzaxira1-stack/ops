apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-node-app
spec:
  rules:
    - host: devops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-node-app
                port:
                  number: 80
